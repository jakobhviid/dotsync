[package]
name = "dotsync-core"
description = "The library behind the dotsync CLI: cloud-folder discovery, mappings, symlink planning, and apply/doctor logic."
version.workspace = true
edition.workspace = true
license.workspace = true
homepage.workspace = true

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
serde_json = "1"

[lints]
workspace = true

[dev-dependencies]
tempfile = "3"
